# AI Toolkit Rules

This is a placeholder rules block installed by `@darek-fp/ai-toolkit`. It is
appended into your project's `AGENTS.md`/`CLAUDE.md` between sentinel markers
so it can be updated or removed cleanly on future installs/uninstalls.
